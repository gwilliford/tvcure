i indexes time from 1 to k
Theta = Lambda, beta, gamma

inputs:
	X matrices 
	Gamma estimates
	n
	
bkj1 <-	(1/n) sum(
				for (i = 1:n) {
					(exp(t(X[i])g)/(1 + exp(t(X[i]g) - psi(y, delta, Z, theta))^2(Xik*Xij)
				)